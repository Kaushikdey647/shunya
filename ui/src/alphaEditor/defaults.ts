/** Editor-visible body only (no import / def); persisted source is wrapped server-side. */
export const DEFAULT_ALPHA_BODY = `return cs.rank(ctx.close)`

/** @deprecated Use DEFAULT_ALPHA_BODY; full module for one-off tests only. */
export const DEFAULT_ALPHA_SOURCE = DEFAULT_ALPHA_BODY
